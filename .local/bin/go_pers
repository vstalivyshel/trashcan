#!/bin/env bash

pers_dirs=""
pers_dirs+="$HOME/HoneyBunny "
pers_dirs+="$HOME/Downloads "

selected=$(find $pers_dirs -maxdepth 1 -type d | fzf)

if [[ -z $selected ]]; then
  exit 0
fi

cd $selected; $EDITOR;